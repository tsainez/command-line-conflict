[pytest]
pythonpath = .
testpaths = tests
norecursedirs = fonts
addopts = -n auto --strict-markers
xfail_strict = true
markers =
    unit: Unit tests (fast, isolated)
    integration: Integration tests (slower, involve multiple components)
    security: Security-focused tests
    slow: Slow running tests
